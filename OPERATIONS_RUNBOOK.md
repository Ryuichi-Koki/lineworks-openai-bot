# 本番運用・障害対応ランブック

更新日: 2026-07-27

## 日次確認

1. VercelのProduction DeploymentがReadyであること。
2. Vercel LogsでLINE、LINE WORKS、Stripe Webhookの5xxがないこと。
3. Stripe WorkbenchでWebhook失敗・再試行がないこと。
4. Supabaseで接続数、容量、エラーが急増していないこと。
5. LINE WORKSの相談依頼に未対応がないこと。

公開ページの疎通は `pnpm check:production:public` で確認します。

## 監視と担当

| 項目 | 一次対応者 | 通知先 | 目標初動 |
|---|---|---|---|
| LINE無応答 | 要記入 | 要記入 | 30分 |
| Stripe決済・Webhook | 要記入 | 要記入 | 30分 |
| Supabase障害 | 要記入 | 要記入 | 30分 |
| 個人情報・誤送信 | 要記入 | 要記入 | 即時 |
| 税理士相談滞留 | 要記入 | 要記入 | 営業時間内1時間 |

担当者と通知先が未記入のまま全面公開しないでください。

## 障害時の停止順

1. 決済障害では `STRIPE_BILLING_ENABLED=false` にして再デプロイする。
2. 会員反映障害では `MEMBERSHIP_BILLING_ENABLED=false` にする。
3. LINE障害ではLINE DevelopersのWebhookを一時停止し、障害告知を行う。
4. 直前デプロイ起因ならVercelで直前の正常Deploymentへロールバックする。
5. Stripeイベントは削除せず、イベントIDと処理結果を保存してから再送する。

## 個人情報・セキュリティ事故

1. 対象機能を停止し、ログの保存期間を延長する。
2. 氏名・住所・電話番号・マイナンバー・口座・カード情報をチャットへ再掲しない。
3. 影響ユーザー、期間、閲覧者、外部提供先を特定する。
4. 個人情報保護責任者と顧問へ連絡し、通知・報告要否を判断する。
5. APIキー流出時は対象キーを失効・再発行し、Git履歴とログを再検査する。

## 利用者データの削除依頼

1. LINEユーザーIDと本人確認方法を記録する。
2. Stripeの法定保存対象を確認し、即時削除対象と保存対象を分ける。
3. Supabaseの会員・利用台帳・相談受付を対象IDで特定する。
4. 削除または匿名化を実施し、実施者・日時・対象を監査記録へ残す。
5. LINE上で完了通知する。機密データの内容は通知へ含めない。

## リリース記録

各リリースで、Git commit、Vercel Deployment URL、環境変数変更、DB migration、
E2E結果、承認者を1つの記録へ残します。
