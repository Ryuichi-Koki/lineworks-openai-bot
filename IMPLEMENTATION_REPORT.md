# 公式LINE税務相談AI 回答ロジック改修報告

作成日: 2026-07-23
実装先: `lineworks-openai-bot-dev`（開発用コピー）
本番反映: 未実施

## 1. 改修前の問題点

元コードの調査時点で確認した、改修前のフローは次のとおりです。以下は現在の実装状態ではありません。

1. LINE受信: `app/api/line/callback/route.ts`
2. AIシステムプロンプト: `lib/openai/generateReplyDraft.ts` へ長文を直接記載
3. 根拠検索: 改修前は未実装。改修前プロンプトにも「外部の法令データベースを検索できない」と記載
4. 追加質問判定: 明示的なコード判定はなく、モデル指示だけに依存
5. LINE返信: LINE WORKSで承認後、`lib/line/client.ts` のPush APIで送信
6. 会話履歴: Upstash Redis。本番以外はプロセスメモリ。LINE userIdはハッシュ化してキーに使用
7. 出典・更新日: 構造化された管理なし
8. ログ・監査: Webhookエラーの安全なconsoleログと案件状態はあるが、回答根拠・モデル・プロンプト版を含む監査履歴はなし

改修前の主因は、検索できない状態で根拠の厳密さを求め、一般回答を保証する後処理もなかったことです。現在は、e-Gov法令検索等の公式ドメイン限定Web検索を実装しています。

## 2. 変更したファイル

### 新規

- `prompts/system_prompt.md`
- `prompts/answer_policy.md`
- `prompts/examples.md`
- `prompts/source_policy.md`
- `lib/openai/promptLoader.ts`
- `lib/security/redaction.ts`
- `lib/tax/policy.ts`
- `tests/policy.test.ts`
- `tests/webhook-regression.test.ts`
- `scripts/eval-tax-answers.ts`
- `eslint.config.mjs`
- `IMPLEMENTATION_REPORT.md`

### 変更

- `lib/openai/generateReplyDraft.ts`
- `lib/approvals/store.ts`
- `lib/line/client.ts`
- `lib/lineworks/client.ts`
- `app/api/line/callback/route.ts`
- `app/api/lineworks/callback/route.ts`
- `.env.example`
- `README.md`
- `package.json`
- `pnpm-lock.yaml`
- `tsconfig.json`

## 3. 変更概要

- 長文プロンプトを4ファイルへ分離し、内容ハッシュからプロンプト版を生成
- 回答レベルA/B/C、信頼度（高・中・低）、根拠検証状態を構造化出力へ追加
- 一般回答なしで追加質問だけを返した場合、強化指示付きで一度再生成
- 確認事項を最大4件へ制限
- 不正相談と重要な個別判断案件をローカルルールでもレベルCへ強制
- 公式根拠が検証できない回答を信頼度「低」・税理士確認対象へ強制
- 回答本文をLINE最大3通へ分割し、送信直前にも機密情報をマスク
- 顧問先プロファイルの任意参照と参照項目記録を追加
- 案件単位の監査ログを追加
- Next.js 16とのLint互換性のためESLintを9.39.1へ調整

## 4. 新しい回答判定フロー

```text
LINE質問受信・署名検証
  ↓
会話履歴と登録済み顧問先プロファイルを取得
  ↓
機密情報をマスキング
  ↓
不正・重要個別判断のローカル判定
  ↓
個人情報を含まない一般化検索語を生成
  ↓
e-Gov・国税庁等の公式ドメインだけをWeb検索
  ↓
回答レベル、前提、条件分岐、確認事項、出典、信頼度を構造化生成
  ↓
検索注釈に実在する公式URLだけを出典として採用
  ↓
追加質問だけなら一般回答を必須として一度再生成
  ↓
根拠なし・低信頼・レベルCは税理士確認対象へ強制
  ↓
監査ログ保存 → LINE WORKSで人が確認
  ↓
承認後のみ、マスキング・最大3通分割してLINE Push送信
```

## 5. システムプロンプト全文

実際にAPIへ渡す全文は、次の4ファイルをこの順で連結したものです。

1. `prompts/system_prompt.md`
2. `prompts/answer_policy.md`
3. `prompts/source_policy.md`
4. `prompts/examples.md`

回答日（日本時間）は実行時に末尾へ追加します。4ファイルのSHA-256から
`tax-policy-<先頭12桁>` の版番号を生成し、監査ログへ保存します。

## 6. 根拠資料検索

既存回答モデル `OPENAI_MODEL` は維持し、根拠検索だけを
`OPENAI_TAX_SEARCH_MODEL`（既定 `gpt-5-mini`）へ分離しました。

1. 検索ツールなしで、顧問先固有情報を含まない一般化検索語を1件生成
2. Web検索を最大4回に制限
3. 許可ドメインを現行e-Gov法令検索 `laws.e-gov.go.jp`（旧 `elaws.e-gov.go.jp` も互換許可）、国税庁、総務省、裁判所、国税不服審判所および設定済み地方公共団体に限定
4. 国税庁では法令解釈通達、その他法令解釈情報、事務運営指針、告示、文書回答事例、質疑応答事例の公式入口とその配下を優先検索
5. 裁決事例が必要な場合は、国税不服審判所の公表裁決事例集 `https://www.kfs.go.jp/service/` とその配下を参照
6. 裁決は個別事案の参考資料として扱い、法令・告示・通達より優先せず、事実関係を無視して一般化しない
7. 検索結果本文を回答モデルへ根拠資料として渡す
8. 回答モデルが返したURLと検索レスポンスの引用注釈URLを照合
9. 一致しないURL、民間URL、不正URLは破棄
10. 根拠ゼロなら「未検証」・信頼度「低」・税理士確認対象へ強制

各出典は、資料名、URL、資料種別、法令・条文等、公開日、施行日、更新日、
取得日時、根拠箇所の短い引用・要旨を保持できます。

現時点ではOpenAI Responses APIのWeb検索を経由してe-Govの公開ページを検索・取得します。
e-Gov法令API Version 1/2を直接呼び出す専用クライアントは未実装です。条文番号・基準日時点の
本文をより厳密に機械照合する場合は、法令API Version 2の直接連携を追加する余地があります。

## 7. セキュリティ

- LINE・LINE WORKSのraw body署名検証を維持
- マイナンバー、口座、カード、メール、電話、APIキー、秘密鍵、健康情報をモデル送信前にマスク
- LINE送信直前にも再マスク
- Web検索語から会社名形式、長い番号、機密パターンを除去
- ユーザー文、会話履歴、顧問先情報、検索資料を命令ではなく事実資料としてXML風境界で分離
- 不正相談をコードでも検知し、レベルCへ強制
- 検索注釈で確認できないURLを破棄
- 顧問先情報は登録項目だけを使用し、使用項目を監査ログへ記録
- 担当者IDは監査ログでハッシュ化
- APIキー、元の`.env.local`、秘密鍵は開発コピーへ複製していない

## 8. テストケース

### 実モデル評価

- 一般回答を先に返すべき曖昧な質問15件
- 不正を支援せず適正申告へ案内すべき質問5件
- 曖昧質問ごとの主要税務論点（固定資産・減価償却、交際費、源泉徴収、事前確定届出給与等）
- 公式Web検索あり全20件

### 単体・回帰

- 曖昧質問の不正誤判定防止
- 不正質問5件のレベルC強制
- 追加質問だけの回答検出
- 根拠未検証時の低信頼・税理士確認強制
- レベルC本文の税理士確認、不正相談本文の適正申告案内の強制
- 公式URL・検索引用注釈の照合
- 現行e-Gov、国税庁公式入口、国税不服審判所公式入口の許可
- 追跡パラメータを除いたURL正規化
- 機密情報マスキング
- 4分割プロンプト契約
- LINE・LINE WORKS署名検証
- Webhook重複案件防止と承認状態遷移
- 会話履歴・監査履歴
- LINE最大3通分割

## 9. テスト結果

| 検証 | 結果 |
|---|---:|
| 単体・回帰テスト | 15/15 合格 |
| 曖昧質問の実モデル・主要論点評価（公式検索あり） | 15/15 合格 |
| 不正相談の拒否・適正申告評価（公式検索あり） | 5/5 合格 |
| 出典0件だった回答の個別再検索 | 2/2 合格 |
| TypeScript型チェック | 合格 |
| ESLint | 合格（警告なし） |
| Next.js本番ビルド | 合格 |

曖昧質問15件は、追加質問だけで終わらず、質問別の主要税務論点を含みました。
不正相談5件はすべてレベルCとなり、具体的な発見回避手順を示さず、
適正申告と税理士確認を案内しました。初回検索で出典0件だった役員賞与と
インボイスなし領収書は、個別再検索でそれぞれ検証済み2件、部分検証1件を取得しました。

実際のLINE・LINE WORKS外部送信は、顧問先や職員への通知を発生させるため実施していません。
署名、保存、状態遷移、ビルドで回帰確認しています。

## 10. 残課題

- 管理画面は元システムに存在しないため、信頼度はRedis監査ログとLINE WORKS承認画面で確認する形
- 顧問先プロファイルを登録・更新する管理UIまたは同期バッチは未実装
- 地方公共団体の許可ドメインは利用地域に応じた追加が必要
- 条文名と条番号の意味的一致は検索根拠とモデル指示で確認しているが、法令XMLを使った完全な機械検証ではない
- 3段階API処理により、従来より回答時間とAPIコストが増える
- Web検索結果には一時的な揺らぎがあり、明確な公式資料が存在しても出典0件になる場合がある。その場合は低信頼・税理士確認へ安全側に倒れるが、検索再試行や税目別固定検索語の追加余地がある
- 添付書類のOCR・安全な受領は対象外
- OpenAI公式Docs MCPは環境の`codex.exe`実行制限で追加できず、公式OpenAI Webドキュメントで仕様確認した

## 11. 本番反映前の人による確認

1. 税理士が4プロンプト全文と回答例をレビューする
2. 公式検索の許可ドメインに必要な都道府県・市区町村を追加する
3. `OPENAI_TAX_SEARCH_MODEL` の利用可否、費用、レート制限を確認する
4. Redisの顧問先プロファイル形式と既存マスタの連携方法を決める
5. 監査ログの保存期間、閲覧権限、削除手順を個人情報保護方針に合わせる
6. テスト用LINE・LINE WORKS環境で受信、承認、修正、却下、送信をE2E確認する
7. 回答遅延がLINE Webhookのタイムアウト・再送条件に影響しないか負荷試験する
8. 本番環境変数を追加し、開発コピーとの差分をレビューしてから反映する
