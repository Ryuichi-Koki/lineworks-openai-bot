# リリース手順書：UIレビュー対応（2026-07-29）

作成日: 2026-07-29
対象: Vercel `lineworks-openai-bot-mfrs` / Supabase 本番 / LINE公式アカウント

本手順は **Vercel と Supabase の管理画面から直接操作する** 前提で書いています。
本番の認証情報は開発機に置かず、Vercel の Environment Variables にのみ保持する
（`VERCEL_SETUP_CHECKLIST.md` の方針）に沿います。

---

## 0. 事前確認（Vercel ダッシュボード）

Settings → Environment Variables → **Production** で次を確認する。
ここが誤っていると、以降の作業をしても利用者には届かない。

| 変数 | 確認内容 |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | **本番アカウントのトークンか**（開発用 `@604pzkll` / `GPT-san` ではないこと） |
| `LINE_CHANNEL_SECRET` | 上記と同じチャネルのものか |
| `LINE_RICH_MENU_EXPECTED_BASIC_ID` | 本番のベーシックID（`@` から始まる）が設定されているか |
| `MEMBERSHIP_BILLING_ENABLED` | 現在の値。`false` の場合、後述の機能が動作しない |
| `STRIPE_BILLING_ENABLED` | 現在の値 |
| `DATABASE_URL` | Supabase 本番の Transaction Pooler（port 6543）か |

### フラグが `false` のときに動作しないもの

`MEMBERSHIP_BILLING_ENABLED=false` の場合、以下は無効のままとなる。

- 規約同意の要求と会員登録導線
- 登録前に届いた質問の預かりと自動回答
- 利用回数の制限、残数表示、マイページ
- 税理士相談の受付・枠管理・内容確認
- LINE Webhook の重複排除

一方、**Stripe Webhook からの決済完了通知はこのフラグの外**にあるため、
Stripe が稼働していれば動作する。

---

## 1. Supabase 本番へ migration を適用

Supabase ダッシュボード → 対象プロジェクト → **SQL Editor** で次を実行する。
追加専用・冪等であり、既存データを変更しない。何度実行しても安全。

```sql
begin;

create table if not exists pending_questions (
  line_user_id text primary key,
  question text not null,
  created_at timestamptz not null default now(),
  expires_at timestamptz not null default (now() + interval '24 hours')
);

create index if not exists pending_questions_expires_at_idx
  on pending_questions (expires_at);

alter table pending_questions enable row level security;

commit;
```

### 適用確認

```sql
select tablename
from pg_catalog.pg_tables
where schemaname = 'public'
  and tablename in (
    'plans','users','review_requests','usage_events','webhook_events',
    'admin_audit_logs','stripe_billing_objects','tax_review_intakes',
    'policy_acceptances','pending_questions'
  )
order by tablename;
```

10行返れば完了。`pending_questions` が含まれることを確認する。

> このテーブルは `users` への外部キーを持たない。会員登録が済む前の利用者の
> 質問を預かるため、意図的にそうしている。期限切れ行は保存のたびに削除される。

---

## 2. Vercel へデプロイ

**手順1の完了後に行う。** 順序を逆にすると、未同意者が質問した際に
預かり処理が失敗する（同意カード自体は表示されるため、従来の挙動に戻るだけ）。

1. 対象ブランチを Production へデプロイする
2. Deployment のビルドログで `Compiled successfully` を確認する
3. `https://bot.abtax.jp/terms` が 200 で表示されることを確認する

ビルドは開発機で検証済み（13ページ生成、typecheck・lint・テスト103件すべて通過）。

### デプロイ後の確認（LINE実機）

現行のリッチメニュー（旧4分割）のままでも動作する。新コードは旧メニューの
postback（`select_paid_membership` / `select_free_membership` /
`start_tax_review_intake`）と「退会したい」テキストを引き続き処理する。

- [ ] 質問を送信 → 「ご質問を受け付けました」→ AI回答が届く
- [ ] AI回答の冒頭に `※AIによる自動回答です` が付く
- [ ] 「料金を教えて」→ 料金表が届き、**決済ページは作られない**
- [ ] 「退会したい」→ Stripe 契約管理へのボタンが届く
- [ ] Stripe テストモードで決済 → **LINEに完了メッセージが届く**（最重要）

---

## 3. LINE リッチメニューを適用

**手順2の完了後に行う。** 逆順にすると新メニューの
`show_pricing` / `show_status` / `open_billing_portal` / `show_legal` /
`start_question` が本番コードに存在せず、押しても無反応になる。

リッチメニューの postback 設定は LINE Official Account Manager では
行えないため、**Messaging API 経由（付属スクリプト）が必要**。

認証情報をファイルに保存せず、実行時のみ環境変数で渡す。

```bash
# 1) 対象アカウントの確認（読み取りのみ・変更なし）
LINE_CHANNEL_ACCESS_TOKEN='<本番トークン>' \
LINE_RICH_MENU_EXPECTED_BASIC_ID='<本番ベーシックID>' \
node scripts/setup-line-rich-menu.mjs --status
```

出力の `displayName` が本番アカウント名であることを必ず確認する。
`defaultRichMenuId` は控えておく（ロールバック用）。

```bash
# 2) 適用（アカウント全体のデフォルトメニューを差し替える）
LINE_CHANNEL_ACCESS_TOKEN='<本番トークン>' \
LINE_RICH_MENU_EXPECTED_BASIC_ID='<本番ベーシックID>' \
node scripts/setup-line-rich-menu.mjs --apply --confirm-account-wide-change
```

ベーシックIDが一致しなければスクリプトが自動で停止する。
適用結果は `.tools/line-rich-menu-last.json` に記録される。

### 新メニューの構成（2500×1686・6分割）

| 位置 | ラベル | 動作 |
|---|---|---|
| 左上 | 質問する | 質問方法の案内 |
| 中上 | 料金プラン | 料金表示（決済ページは作らない） |
| 右上 | 税理士に相談 | 相談内容の入力受付 |
| 左下 | マイページ | 残回数・更新日（回数を消費しない） |
| 中下 | 契約管理 | Stripeポータル（解約フロー直行ではない） |
| 右下 | 規約・ヘルプ | 規約一覧 |

上部130pxの注意表示帯にはタップ領域を割り当てていない。
全ボタンが postback のため、押しても利用者の発言としては残らない。

### 適用後の確認

- [ ] 6つのボタンがすべて反応する
- [ ] 「料金プラン」を押しても自分の発言に「有料会員の登録手続きへ進みます」と出ない
- [ ] 「契約管理」を押しても自分の発言に「退会したい」と出ない
- [ ] 「マイページ」で残回数が表示され、AI回答の回数が減らない

---

## ロールバック

### コード

Vercel の Deployments から直前のデプロイへ Instant Rollback する。
migration 005 を戻す必要はない（旧コードは `pending_questions` を参照しないため、
テーブルが残っていても無害）。

### リッチメニュー

適用前の `--status` で控えた ID、または
`.tools/line-rich-menu-last.json` の `previousDefaultRichMenuId` に戻す。

```bash
curl -X POST \
  -H "Authorization: Bearer <本番トークン>" \
  "https://api.line.me/v2/bot/user/all/richmenu/<戻したいrichMenuId>"
```

> 開発用アカウント（`@604pzkll`）の記録値:
> 現行 `richmenu-f1b231ad9a7629f21c078a9d4a4b6392` /
> その前 `richmenu-685f73baac7eeb5d8f3dbf86fda44610`
> **本番アカウントのIDは別物**なので、必ず手順3の `--status` で確認した値を使う。

---

## 本リリースの内容

UIレビューで指摘した20項目すべてに対応。主なもの:

| 分類 | 内容 |
|---|---|
| Critical | 決済完了後にLINEへ通知が届かなかった問題を修正 |
| Critical | 「料金を見る」が実際は申し込み導線だった問題を分離 |
| Critical | 税理士の回答とAI回答が区別できなかった問題に識別表示を追加 |
| Critical | 相談内容の確認が160字で切れていた問題を全文表示に変更 |
| Critical | 連打で複数のStripe決済ページが作られる問題を防止 |
| High | 同意前に送った質問が破棄されていた問題（預かり→自動回答） |
| High | 税務質問が料金・退会案内へ誤誘導される問題を抑制 |
| High | 会員状態を確認する手段（マイページ）を新設 |
| Medium | 用語統一、同意ボタンの表現、期限切れ案内、エラー時の通知、長文の切り捨て、アクセシビリティ |

### 未了（法務確認事項）

- **特商法表記の用語**：UIは「税理士相談」に統一したが、特商法表記は
  「税理士確認」のまま。同一の役務を指すが、表示義務のある文書のため
  弁護士確認のうえ統一の要否を判断すること。法務文書は変更していない。
- **回答主体の表示方法**：税理士法上の論点を含む。文言は利用規約の
  「当法人の社員税理士又は所属税理士が確認します」に合わせている。
- **承認モードの表記**：「当法人の担当者が確認」としている。承認者が
  全員税理士である運用であれば「当法人の税理士が確認」に変更可能。
